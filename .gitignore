# Deploy credentials — never commit
.deploy-token
.deploy-token.txt
*.token

# Local working files
versions/
thumbnail.*
*.tmp

# Claude Code session data
.claude/

# OS noise
.DS_Store
Thumbs.db
desktop.ini
